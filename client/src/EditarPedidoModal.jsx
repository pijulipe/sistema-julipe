import React from "react";
import FormularioPedido from "./FormularioPedido.jsx";
export default function EditarPedidoModal({ pedido, onClose }) { return <FormularioPedido pedido={pedido} onClose={onClose} />; }
